// Question.jsx
const displayQuestion = (category) => {
    // Code pour afficher une question de la catégorie sélectionnée
    console.log(`Question de la catégorie ${category}`);
    // Par exemple, vous pouvez afficher la question dans la console pour le moment
};

export { displayQuestion };
